name: Run Trading Result

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-result:
    runs-on: ubuntu-latest

    steps:
      - name: Call Result API
        run: |
          curl -L -A "Mozilla/5.0" https://bet999.gamer.gd/result.php
